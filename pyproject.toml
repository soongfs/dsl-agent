[project]
name = "dsl-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "openai>=1.42.0",
]

[project.optional-dependencies]
dev = ["pytest>=9.0.1"]
